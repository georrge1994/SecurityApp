<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.georrge.securityapps.activity.MainActivity">

    <!-- update button -->
    <Button
        android:layout_alignParentTop="true"
        android:layout_alignParentEnd="true"
        android:id="@+id/main_updateList_button"
        android:background="@drawable/update_icon_selector"
        android:layout_height="50dp"
        android:layout_width="50dp"
        android:onClick="updateListInstallApps"/>

    <!-- List view -->
    <ListView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:id="@+id/main_appList"
        android:divider="#cccccc"
        android:dividerHeight="1px"
        android:layout_below="@+id/main_updateList_button"
        android:layout_alignParentStart="true"/>
</RelativeLayout>
